package main

import (
	"fmt"
	"time"
)

func main() {
	start := time.Now()
	fmt.Println(start)
	// r := new(big.Int)
	// fmt.Println(r.Binomial(1000, 10))

	// elapsed := time.Since(start)
	// log.Printf("Binomial took %s", elapsed)
}
